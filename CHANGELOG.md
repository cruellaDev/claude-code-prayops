# Changelog

## v0.5.0 — 2026-08-07

### 고친 것

- **들여쓰기가 통째로 사라지고 있었다.** Claude Code는 상태줄 각 줄의 앞 공백을 지운다. 그래서 향로가 아무리 정확히 가운데 맞춰져 있어도 화면에서는 모든 줄이 왼쪽 끝에 붙어, 향과 발이 몸통에서 떨어져 나온 막대처럼 보였다. 세 번에 걸쳐 축을 셀 단위·글리프 단위로 맞췄지만 정작 원인은 그림이 아니라 **호스트가 줄을 어떻게 다루는가**였다. 앞쪽 채움을 `U+2800`(점자 빈 칸)으로 바꿨다 — 화면에는 빈 칸이지만 공백이 아니라 지워지지 않는다. 줄 안쪽 공백은 지워지지 않으므로 그대로 둔다.

### 바뀐 것

- **향을 길게.** 한 줄에서 두 줄로. 참고 사진들의 비율에 가깝고, 생일 초처럼 보이던 것이 향으로 읽힌다.
- **켜고 끄는 스위치.** `statusline scope --off` / `--on`. `uninstall`과 다르다 — `uninstall`은 `settings.json`에서 설정을 지우고 이전 상태로 되돌리지만, `--off`는 설정을 남긴 채 그리지만 않는다. 껐다 켜도 범위 선택이 유지된다. 스위치는 스위치여야 한다.
- **창 하나만.** `statusline scope --only-session`. 같은 저장소를 두 창에서 열면 프로젝트 범위로는 둘을 구별할 수 없다. 세션 ID는 창과 함께 죽으므로, 죽은 것이 남아 향로를 영영 숨기지 않도록 `SessionEnd` 훅이 목록에서 지운다.
- **범위 보고가 거짓말하던 것.** 창 하나로 좁혀 둔 상태에서도 "모든 프로젝트에 그린다"고 답했다 — 조건 순서 때문에 세션 분기가 도달 불가능했다. 이제 걸려 있는 규칙을 전부 나열한다.
- **README를 배포 내용에 맞췄다.** 첫 그림이 아직 한자가 붙은 예전 향로였고, 지원 플랫폼 표와 문제 해결 표에 사라진 다운로드 절차가 남아 있었다.

## v0.4.1 — 2026-08-07

향로를 참고 사진에 맞춰 다시 그리고, 두 리뷰가 찾은 반 칸 어긋남을 고쳤다.

### 바뀐 것

- **향로 모양.** 둥글고 불룩한 몸통, 작은 입, 양옆 고리 귀, 짧은 다리 셋. 사용자가 고른 실물 사진과 벡터 그림의 실루엣이다.
- **향과 불씨.** 향 셋이 꽂혀 있고 끝에 빨간 불씨가 붙는다. 색은 그 한 셀에만 입힌다 — 줄 전체를 감싸면 향로까지 물든다.

### 고친 것

- **글리프가 칸 안에서 어디에 그려지는지를 보지 않았다.** 줄마다 "몇 번째 칸"만 맞추고 통과시켰는데, 블록 글리프는 칸의 일부만 칠한다. `▏`는 칸의 왼쪽 1/8이라 향이 불씨보다 0.44칸 왼쪽에 섰고, `▘`는 좌상단 1/4이라 왼발이 받침 밖으로 삐져나오고 가운데 발이 축에서 1/4칸 벗어났다. 칸 중앙에 오는 `▮`과 좌우 대칭인 `▝ ▀ ▘`로 바꿨다.
- **연기가 다시 흔들렸다.** 윗줄을 좌우 한 칸씩 흩날리게 했더니 가로 중심이 7.0 → 7.5 → 6.5로 프레임마다 움직였다 — "연기 사이즈가 자꾸 바뀌어서 정신 사납다"던 그 증상이 세 번째로 돌아온 것이다. 이제 위치는 완전히 고정하고 짙기만 변한다. `TestSmokeNeverMoves`가 120초 동안 연기가 차지한 셀이 한 번도 바뀌지 않으면서도 무늬는 네 가지 이상 나오는지 검사한다.

## v0.4.0 — 2026-08-07

그림을 다시 그렸다.

### 바뀐 것

- **향로를 작고 단순하게.** 폭 21 → 11. 다리 셋, 양쪽 귀, 보이는 재, 아가리보다 불룩한 몸통은 그대로다.
- **줄마다 중심축이 달랐다.** 이전 그림은 각 줄이 8.0 / 9.0 / 10.0 / 9.0 / 9.0 / 9.5 칸에 중심이 있었다 — 모양이 아니라 흔들림으로 읽혔다. 이제 모든 줄이 같은 축이고, 테스트가 이를 고정한다.
- **장면 높이가 변하지 않는다.** 연기가 무작위 줄에 떨어져서, 맨 윗줄에 아무것도 안 걸리면 그 줄이 사라졌다 — 300프레임 중 23프레임에서 높이가 11 → 10 → 9로 바뀌었고, 그때마다 아래 입력창이 움직였다. 연기는 이제 한 줄이고 항상 그려진다.
- **연기가 좌우로 흔들리지 않는다.** 해시로 뽑은 오프셋만큼 옆으로 밀려서 매초 향로의 축을 벗어났다. 이제 향로 다리와 같은 칸에 고정되고, 짙기만 변한다.
- **기도가 인스타그램 하트처럼 터진다.** 이전에는 매 프레임 위치를 새로 뽑았다 — 점들이 자리만 바꿀 뿐 어디로도 가지 않아 잡음처럼 읽혔다. 이제 씨앗이 프레임이 아니라 기도다. 각 이모지가 자기 방향과 속도를 5초 내내 유지하며 향로에서 바깥으로 날아가고, 저마다 다른 순간에 사라진다. 최대 44개.
- **기도가 향로 위에도 겹친다.** 향로 자리를 비워 두던 규칙을 없앴다. 그림은 깔끔했지만 폭죽에 울타리를 친 꼴이었다.
- **한자를 뺐다.** `祈 PrayOps · WORKING · 香 100%` → `PrayOps · WORKING · 100%`. 향로가 동아시아 물건이라는 것 말고는 이유가 없었고, 그 글자를 모르는 사람에게는 아무것도 전하지 못한다.

## v0.3.1 — 2026-08-06

리뷰가 v0.3.0에서 세 가지를 찾았다. 두 개는 예전 결함의 재발이고, 하나는 **되지도 않는 기능을 된다고 적어 둔 것**이다.

### 고친 것

- **"이 프로젝트에서만"이 아예 동작하지 않았다.** v0.3.0은 상태줄 설정을 프로젝트의 `.claude/settings.local.json`에 쓰라고 안내했다. Claude Code는 `statusLine`을 **사용자 `settings.json`에서만** 읽는다 — 프로젝트 설정의 것은 아무 말 없이 무시된다. 그래서 "이 프로젝트만"을 고른 사용자는 **어디에서도 향로를 못 보게 됐고**, 아무 오류도 나지 않았다. 실측으로 확인했다: `refreshInterval: 1`인데 6초 동안 상태줄 명령이 한 번도 실행되지 않았다. 이제 상태줄은 항상 사용자 설정에 두고, 범위는 런타임이 판단한다 — 상태줄 payload의 `workspace.project_dir`를 읽어 allowlist와 대조한다. `prayops statusline scope --only-here` / `--everywhere` / `--not-here`. 저장하는 것은 경로가 아니라 project key hash다.
- **`ensure-runtime.sh`가 다른 플러그인의 디렉터리에 설치했다.** `CLAUDE_PLUGIN_DATA`를 검증 없이 신뢰했는데, doctor와 setup 스킬이 이 스크립트를 Bash 도구에서 실행하라고 안내한다 — 거기서 그 변수는 남의 디렉터리다. 게다가 `runtime.json`까지 남겨서, 그 디렉터리가 **정상 설치인 것처럼 스스로를 검증**하게 만들었다.
- **런처가 물려받은 값을 그대로 넘겼다.** 첫 세션에는 우리 디렉터리에 바이너리가 아직 없어 플러그인 안의 것이 실행되는데, 그건 `runtime/<os>_<arch>/`에 있어서 자기 위치로 데이터 디렉터리를 알아낼 수 없다. 그래서 "플러그인 설치 후 같은 세션에서 `/pray`"라는 **표준 흐름**에서 세션 상태 전체가 남의 디렉터리로 갔다.
- 위 둘의 규칙을 `scripts/plugin-data.sh` 한 곳으로 모았다. 규칙이 `setup.sh`에만 있었고 나머지가 각자 하고 있던 것이 재발의 원인이었다.
- 중단된 복사가 임시 파일을 남기던 것, `runtime.json`을 원자적으로 쓰지 않던 것, `pluginVersion`에 런타임 버전을 적던 것.

### 문서

- README가 여전히 사라진 다운로드 절차를 가르치고 있었다. `docs/05_RUNTIME_BOOTSTRAP.md`는 대체됨으로 표시했고, 뒤집힌 FR-020·FR-053과 D-010·D-012에 개정 주석을 달았다. 새 결정 D-060..D-063을 기록했다.

## v0.3.0 — 2026-08-06

설치가 설치로 끝난다.

### 바뀐 것

- **런타임이 플러그인 안에 들어왔다.** 설치는 마켓플레이스 등록 → 플러그인 설치 → 스킬 실행 → 다운로드 계획 확인 → 승인, 다섯 단계였고 업데이트마다 그중 둘을 다시 밟아야 했다. 다운로드가 있었던 이유는 런타임이 컴파일된 바이너리이고 마켓플레이스는 git clone이기 때문인데, Claude Code는 마켓플레이스를 **shallow clone**한다 — 커밋된 바이너리는 사용자에게 최신 커밋 한 번의 비용만 남긴다. 4개 플랫폼 합쳐 디스크 19MB, 전송 7MB다. 이제 SessionStart 훅이 알맞은 바이너리를 제자리에 복사한다. **업데이트는 `/plugin update` 하나로 끝난다.**
- **"훅은 런타임을 설치하지 않는다"는 규칙의 재해석.** 그 규칙은 인터넷에서 코드를 받아오는 것에 관한 것이었다. 방금 설치한 플러그인 안의 파일을 복사하는 건 그것이 아니고, 별도의 동의를 물을 이유도 없다.
- **커밋된 바이너리는 조용히 소스와 어긋날 수 있다.** 그래서 빌드를 재현 가능하게 만들고(`-trimpath`, 툴체인 고정) CI가 넷을 다시 빌드해 바이트 단위로 비교한다.
- **`/pray`가 대화창에도 향로를 그린다.** 상태줄은 좁고 놓치기 쉽다. `/context`처럼 부를 때마다 대화창에 크게 나타난다.
- **`/prayops:setup`이 상태줄 범위를 묻는다.** 이 프로젝트만(`.claude/settings.local.json`), 전체(사용자 `settings.json`), 또는 켜지 않음.

### 고친 것

- **`ensure-runtime.sh`가 존재하지 않는 플래그로 스모크 테스트를 했다** (`doctor --smoke`, 실제로는 `--bootstrap-smoke`). 훅에서 `|| true`로 감싸여 있었으므로, 아무 소리 없이 아무것도 하지 않는 플러그인이 될 뻔했다.
- **스킬들이 Bash 도구에 없는 환경변수를 썼다.** `$CLAUDE_PLUGIN_DATA`는 훅 안에서만 플러그인별로 설정되고 Bash 도구에서는 다른 플러그인의 디렉터리를 담고 있다. `$CLAUDE_PROJECT_DIR`와 `$CLAUDE_SESSION_ID`는 아예 없다(후자는 `$CLAUDE_CODE_SESSION_ID`다). 이제 스킬은 진짜 데이터 디렉터리를 스스로 찾는 런처를 거치고, 테스트가 스킬이 쓸 수 있는 변수를 고정한다.

## v0.2.2 — 2026-08-06

### 고친 것

- **상태줄이 영원히 IDLE이던 문제.** `CLAUDE_PLUGIN_DATA`는 훅 안에서만 믿을 수 있다 — 거기서는 Claude Code가 플러그인마다 정확한 값을 넣어 준다. 상태줄은 그냥 사용자 설정이고 셸은 그냥 셸이라, 둘 다 부모 프로세스가 들고 있던 값을 물려받는다. 실제 설치에서 그 값은 다른 플러그인의 데이터 디렉터리였다. 아무것도 실패하지 않았다 — 훅은 이쪽에 쓰고 상태줄은 저쪽을 읽어서, 향로가 IDLE에 멈춘 채 `/pray`를 눌러도 기도가 영영 나타나지 않았다. 이제 런타임은 자기가 서 있는 위치(`<데이터>/bin/prayops`)에서 데이터 디렉터리를 알아낸다. 들은 말보다 서 있는 자리가 정확하다. 같은 이유로 `doctor`가 방금 설치한 런타임을 못 찾던 것도 함께 고쳐진다.

## v0.2.1 — 2026-08-06

### 고친 것

- **설치 경로 교정이 너무 넓게 발동하던 것.** v0.2.0은 `CLAUDE_PLUGIN_DATA`의 이름에 `prayops`가 없으면 어디서든 형제 디렉터리를 뒤져 이름이 맞는 곳에 설치했다. 릴리스를 검증하다 이 동작을 그대로 목격했다 — 임시 디렉터리를 지정했는데, 우연히 옆에 있던 문서 사본에 설치됐다. 진짜 누출은 Claude Code의 `.../plugins/data/<마켓>-<플러그인>` 구조 안에서만 일어나므로, 이제 그 구조일 때만 교정한다. 그 밖의 위치는 요청한 곳에 그대로 설치한다.

## v0.2.0 — 2026-08-05

향로가 Claude Code 안으로 들어왔다.

### 바뀐 것

- **향로를 상태줄에 그린다.** Claude Code 상태줄은 `echo` 한 번에 한 행씩 여러 줄을 출력할 수 있다. v0.1.0은 이를 한 줄짜리 표면으로 잘못 알고 제단 전체를 별도 터미널로 밀어냈고, 그래서 "Claude Code 플러그인인데 Claude Code에서 안 보이는" 물건이었다. 이제 설치하면 바로 보인다.
- **블록 문자로 다시 그렸다.** 박스 드로잉(`│ ─ ┌ ┐`)은 도면처럼 보인다. 다리 셋, 벌어진 아가리에 재, 아가리보다 불룩한 몸통, 양쪽 귀를 갖춘 향로를 `█ ▄ ▀ ▐▌ ▒ ░`로 그린다.
- **`/pray`가 기도 이모지를 뿌린다.** 향로 주변에 최대 22개가 나타나 5초에 걸쳐 사그라든다. 향로가 차지한 자리에는 놓지 않는다.
- `prayops watch`는 큰 화면에서 이미지 dissolve까지 보려는 사용자를 위한 부가 기능으로 남는다.

### 고친 것

- **다른 플러그인 디렉터리에 설치되던 결함.** `CLAUDE_PLUGIN_DATA`는 환경에서 오고, 스킬의 Bash 명령은 세션이 들고 있는 값을 그대로 물려받는다. 실제 첫 설치에서 PrayOps가 codex 플러그인의 디렉터리를 가리켰다. 이제 이름이 맞지 않으면 형제 디렉터리에서 진짜 위치를 찾아 교정하고, 교정했다고 알린다.
- **상태줄이 기도를 볼 수 없던 문제.** `pray`가 스풀(읽으면 소멸, watcher 전용)에만 기록해 상태줄에는 영영 보이지 않았다. 이제 세션 파일에도 남기되, 큐가 아니라 마지막 기도 시각을 남긴다 — watcher가 없어도 무한정 쌓이지 않는다.
- **초 단위 깜빡임.** 상태줄은 타이머 외에 이벤트로도 다시 실행된다. 기도 개수를 소수점 나이로 계산해 같은 1초 안에 두 번 실행되면 다른 그림이 나왔다.

### 알려진 한계

- 갱신은 초당 한 프레임이다(`refreshInterval` 최소 1초). 부드러운 애니메이션은 `prayops watch`에만 있다.
- 향로 폭 21칸이 들어가지 않는 좁은 창에서는 한 줄로 물러난다.

## v0.1.0 — 2026-08-04

첫 공개 릴리즈. https://github.com/cruellaDev/claude-code-prayops/releases/tag/v0.1.0

### 추가

- **설치**: `/plugin marketplace add` → `install` → `/prayops:setup`. 플러그인 설치만으로는 아무것도 받지 않고, setup이 설치 계획을 먼저 보여준 뒤 동의를 받아 GitHub Releases에서 이 플랫폼용 바이너리 하나를 SHA-256 검증 후 원자적으로 설치합니다. 체크섬 불일치·아카이브 거부·스모크 실패·동시 실행은 각각 다른 종료 코드로 끝나며, 어느 경우에도 기존 런타임은 그대로입니다.
- **훅**: Claude Code lifecycle 9종을 기록합니다. 런타임이 없으면 조용히 성공하고, 실패해도 항상 exit 0에 무출력이며 이유는 doctor가 읽는 파일 하나에 남습니다. 실측 왕복 9.4ms.
- **상태줄**: `祈 PrayOps · WORKING · 香 64%`. opt-in이며 기존 설정 전체를 백업하고 `uninstall`로 정확히 복원합니다.
- **제단**: `prayops watch`가 향·연기·상태를 별도 터미널에 그립니다. 활성 12fps, 유휴 4fps. `--motion off`와 `NO_COLOR` 지원.
- **기도**: `/prayops:pray`가 텍스트·preset·명시한 로컬 이미지를 향로 옆에 띄웠다가 셀 단위로 흩뜨립니다. 같은 seed는 같은 위치와 같은 dissolve를 재현하고, 리사이즈해도 다시 뽑지 않습니다. 활성 1개, 대기 5개.
- **진단**: `/prayops:doctor`가 플러그인·런타임·버전·스풀·훅·상태줄·별칭·watcher·터미널을 검사합니다. 아무것도 고치지 않습니다.
- **별칭**: 선택적 `/pray`. 이미 존재하면 설치를 거부하고 그 파일을 건드리지 않습니다.

### 개인정보

프롬프트·코드·명령어·tool input/output·오류 메시지·transcript 경로·어시스턴트 메시지·원본 작업 디렉터리를 저장하지 않습니다. 작업 디렉터리는 해시로만, 도구 이름은 분류로만 남습니다. 이 약속은 실제 바이너리에 위 항목이 담긴 payload를 흘려보낸 뒤 디스크에 남은 모든 바이트를 읽는 테스트로 검증합니다. 텔레메트리 없음. 런타임 바이너리에는 HTTP 클라이언트가 링크되지 않습니다.

### 검증한 것

- `docs/02_REQUIREMENTS.md` §12 인수 테스트 12개 중 11개를 `internal/e2e`가 자동 검증한다. 남은 하나는 `prayops watch`의 실제 화면이다.
- 발행된 아카이브 4종 전부: 체크섬 일치, 부트스트랩 항목 규칙 통과, 아키텍처 정확, 전부 정적 링크.
- macOS arm64 — 발행된 릴리즈에서 실제 설치·실행.
- macOS amd64 — 발행된 바이너리를 Rosetta로 실행, 훅·상태줄·기도 확인.
- Linux amd64 — 컨테이너에서 부트스트랩 전 경로(계획 → 설치 → 멱등 재실행)와 훅·상태줄 확인. `wget`과 `shasum`이 없는 최소 이미지에서 `curl`·`sha256sum` 폴백이 동작했다.
- Linux arm64 — 컨테이너에서 실행 확인.

### 아직 확인하지 못한 것

- **실제 터미널에서 `prayops watch`를 사람이 본 적이 없다.** pty를 붙여 동작은 확인했지만, 연기 흐름·dissolve 속도·리사이즈 반응이 보기에 어떤지는 눈으로 봐야 안다. 자동화하지 못한 유일한 인수 테스트다.
- **WSL2.** linux로 처리되므로 위 Linux 검증이 대부분을 덮지만, 파일시스템 경로와 `/dev/tty` 동작이 달라 동일하지는 않다.

### 알려진 한계

- native Windows 미지원 (v0.2 예정). WSL2는 linux로 처리합니다.
- kitty/sixel 이미지 프로토콜 미지원. 이미지는 반블록으로 그립니다.
- ambiguous width 문자를 2칸으로 렌더하도록 설정된 터미널에서는 제단 테두리가 어긋날 수 있습니다.
- Codex 통합(`integrations/codex/`)은 이벤트 모델만 공유하며 아직 동작하지 않습니다.

### 발행 후 수정

첫 v0.1.0 태그는 설치할 수 없었다. GoReleaser가 관례적으로 넣는 `LICENSE`·`README.md`·`CHANGELOG.md`를 부트스트랩의 항목 규칙이 거부했다. 방어는 설계대로 작동했으나 틀린 것을 방어하고 있었다. 규칙을 "어떤 파일을 담았는가"에서 "항목이 어디에 떨어질 수 있는가"로 바꾸고, `release-dry-run`이 아카이브 이름만 보던 것을 내용 검증과 실제 설치까지 하도록 고친 뒤 같은 태그를 재발행했다. 깨진 릴리즈는 발행 20분 만에 삭제했고, 설치 자체가 불가능했으므로 성공한 사용자는 있을 수 없다.
