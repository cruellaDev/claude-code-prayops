# Changelog

## v0.1.0 — 2026-08-04

첫 공개 릴리즈. https://github.com/cruellaDev/claude-code-prayops/releases/tag/v0.1.0

### 추가

- **설치**: `/plugin marketplace add` → `install` → `/prayops:setup`. 플러그인 설치만으로는 아무것도 받지 않고, setup이 설치 계획을 먼저 보여준 뒤 동의를 받아 GitHub Releases에서 이 플랫폼용 바이너리 하나를 SHA-256 검증 후 원자적으로 설치합니다. 체크섬 불일치·아카이브 거부·스모크 실패·동시 실행은 각각 다른 종료 코드로 끝나며, 어느 경우에도 기존 런타임은 그대로입니다.
- **훅**: Claude Code lifecycle 9종을 기록합니다. 런타임이 없으면 조용히 성공하고, 실패해도 항상 exit 0에 무출력이며 이유는 doctor가 읽는 파일 하나에 남습니다. 실측 왕복 9.4ms.
- **상태줄**: `祈 PrayOps · WORKING · 香 64%`. opt-in이며 기존 설정 전체를 백업하고 `uninstall`로 정확히 복원합니다.
- **제단**: `prayops watch`가 향·연기·상태를 별도 터미널에 그립니다. 활성 12fps, 유휴 4fps. `--motion off`와 `NO_COLOR` 지원.
- **기도**: `/prayops:pray`가 텍스트·preset·명시한 로컬 이미지를 향로 옆에 띄웠다가 셀 단위로 흩뜨립니다. 같은 seed는 같은 위치와 같은 dissolve를 재현하고, 리사이즈해도 다시 뽑지 않습니다. 활성 1개, 대기 5개.
- **진단**: `/prayops:doctor`가 플러그인·런타임·버전·스풀·훅·상태줄·별칭·watcher·터미널을 검사합니다. 아무것도 고치지 않습니다.
- **별칭**: 선택적 `/pray`. 이미 존재하면 설치를 거부하고 그 파일을 건드리지 않습니다.

### 개인정보

프롬프트·코드·명령어·tool input/output·오류 메시지·transcript 경로·어시스턴트 메시지·원본 작업 디렉터리를 저장하지 않습니다. 작업 디렉터리는 해시로만, 도구 이름은 분류로만 남습니다. 이 약속은 실제 바이너리에 위 항목이 담긴 payload를 흘려보낸 뒤 디스크에 남은 모든 바이트를 읽는 테스트로 검증합니다. 텔레메트리 없음. 런타임 바이너리에는 HTTP 클라이언트가 링크되지 않습니다.

### 검증한 것

- `docs/02_REQUIREMENTS.md` §12 인수 테스트 12개 중 11개를 `internal/e2e`가 자동 검증한다. 남은 하나는 `prayops watch`의 실제 화면이다.
- 발행된 아카이브 4종 전부: 체크섬 일치, 부트스트랩 항목 규칙 통과, 아키텍처 정확, 전부 정적 링크.
- macOS arm64 — 발행된 릴리즈에서 실제 설치·실행.
- macOS amd64 — 발행된 바이너리를 Rosetta로 실행, 훅·상태줄·기도 확인.
- Linux amd64 — 컨테이너에서 부트스트랩 전 경로(계획 → 설치 → 멱등 재실행)와 훅·상태줄 확인. `wget`과 `shasum`이 없는 최소 이미지에서 `curl`·`sha256sum` 폴백이 동작했다.
- Linux arm64 — 컨테이너에서 실행 확인.

### 아직 확인하지 못한 것

- **실제 터미널에서 `prayops watch`를 사람이 본 적이 없다.** pty를 붙여 동작은 확인했지만, 연기 흐름·dissolve 속도·리사이즈 반응이 보기에 어떤지는 눈으로 봐야 안다. 자동화하지 못한 유일한 인수 테스트다.
- **WSL2.** linux로 처리되므로 위 Linux 검증이 대부분을 덮지만, 파일시스템 경로와 `/dev/tty` 동작이 달라 동일하지는 않다.

### 알려진 한계

- native Windows 미지원 (v0.2 예정). WSL2는 linux로 처리합니다.
- kitty/sixel 이미지 프로토콜 미지원. 이미지는 반블록으로 그립니다.
- ambiguous width 문자를 2칸으로 렌더하도록 설정된 터미널에서는 제단 테두리가 어긋날 수 있습니다.
- Codex 통합(`integrations/codex/`)은 이벤트 모델만 공유하며 아직 동작하지 않습니다.

### 발행 후 수정

첫 v0.1.0 태그는 설치할 수 없었다. GoReleaser가 관례적으로 넣는 `LICENSE`·`README.md`·`CHANGELOG.md`를 부트스트랩의 항목 규칙이 거부했다. 방어는 설계대로 작동했으나 틀린 것을 방어하고 있었다. 규칙을 "어떤 파일을 담았는가"에서 "항목이 어디에 떨어질 수 있는가"로 바꾸고, `release-dry-run`이 아카이브 이름만 보던 것을 내용 검증과 실제 설치까지 하도록 고친 뒤 같은 태그를 재발행했다. 깨진 릴리즈는 발행 20분 만에 삭제했고, 설치 자체가 불가능했으므로 성공한 사용자는 있을 수 없다.
