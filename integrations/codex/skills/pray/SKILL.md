---
name: pray
description: Send a prayer request to an already installed PrayOps runtime.
---

Use only when the user explicitly invokes `$pray`.

Do not install the Claude Code plugin runtime from Codex plugin data.
Run the separately installed `prayops pray` CLI.
